# First-Pass Claim Evidence Matrix

| claim_id | claim_statement | mechanism | code_or_artifact_source | required_experiment_evidence | risk_if_unsupported |
|---|---|---|---|---|---|
| C1 | The method introduces timing pressure directly into placement rather than treating timing as a post hoc check. | Edge slack `T - L_e` changes weights for later placement rounds. | `src/main_td.cpp`, `src/main_singlecol.cpp`, `src/momentum_weighter.h` | Table or figure showing timing-oriented metric improvement versus equal-weight baseline. | Downgrade to `timing-inspired` weighting rather than strong timing-driven phrasing. |
| C2 | The method adaptively focuses optimization on critical edges. | Top-ratio critical-edge selection plus momentum-style weight accumulation. | `src/momentum_weighter.h` | Ablation on top-ratio selection and momentum update. | If unsupported, present as heuristic reweighting without adaptive-focus claim. |
| C3 | The method seeks target-feasible placements under a maximum edge-length budget `T`. | Early stop when `WNS >= 0`; update rule based on violation against `T`. | `src/main_td.cpp`, `src/main_singlecol.cpp` | Sensitivity study over different `T` and feasibility rate. | If unsupported, phrase `T` as tuning knob rather than central method contract. |
| C4 | The placement engine remains legality-aware while timing pressure is applied externally. | Weighted placement solve is separated from update logic; legality check is retained. | `src/main_singlecol.cpp`, `src/lightOCShortest.cpp` | Legality/OC success rate and placement validity discussion. | If unsupported, avoid claiming robust legality preservation. |
| C5 | The method can extend from a single-column base solve to multicolumn realization. | Dedicated multicolumn splitter/replication flow. | `src/multicol_splitter.cpp`, `src/main_multicol.cpp` | Comparison across single-column and multicolumn cases. | If unsupported, keep multicolumn support as implementation detail, not contribution bullet. |
